import * as React from 'react';
import { IconProps } from 'react-magma-icons';
import { ThemeInterface } from '../../theme/magma';
import { Omit, XOR } from '../../utils';
export declare enum TagColor {
    default = "default",
    primary = "primary",
    lowContrast = "lowContrast",
    highContrast = "highContrast"
}
export declare enum TagSize {
    medium = "medium",
    small = "small"
}
/**
 * @children required
 */
export interface BaseTagProps extends Omit<React.HTMLAttributes<HTMLButtonElement>, 'onClick'> {
    /**
     * Color changes between 'primary', 'low contrast', and 'high contrast' style variants between each Tag.
     * @default TagColor.default
     */
    color?: TagColor;
    /**
     * Size toggles between a default, and a small size Tag.
     * @default TagSize.medium
     */
    size?: TagSize;
    /**
     * Gets the active Tag label for use with the aria-label attribute inline for accessibility.
     */
    labelText?: React.ReactNode;
    /**
     * Disabled Tag state.
     */
    disabled?: boolean;
    testId?: string;
    /**
     * Allows passing a Magma icon to the Tag.
     */
    icon?: React.ReactElement<IconProps>;
    /**
     * Passes a clickable state to the Tag.
     */
    isClickable?: boolean;
    /**
     * Allows for Inverse styling of each Tag.
     */
    isInverse?: boolean;
    /**
     * @internal
     */
    theme?: ThemeInterface;
}
export interface DeletableTagProps extends BaseTagProps {
    onDelete: () => void;
}
export interface ClickableTagProps extends BaseTagProps {
    onClick?: () => void;
}
export declare type TagProps = XOR<DeletableTagProps, ClickableTagProps>;
export declare const Tag: React.ForwardRefExoticComponent<({
    onDelete?: never;
} & ClickableTagProps & React.RefAttributes<HTMLButtonElement>) | ({
    onClick?: never;
} & DeletableTagProps & React.RefAttributes<HTMLButtonElement>)>;
