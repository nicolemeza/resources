import * as React from 'react';
import { ButtonProps } from '../Button';
export interface PageButtonProps extends ButtonProps {
    isSelected?: boolean;
}
export declare function pageButtonTypeSize(props: any): string;
export declare const PageButton: React.ForwardRefExoticComponent<PageButtonProps & React.RefAttributes<HTMLButtonElement>>;
