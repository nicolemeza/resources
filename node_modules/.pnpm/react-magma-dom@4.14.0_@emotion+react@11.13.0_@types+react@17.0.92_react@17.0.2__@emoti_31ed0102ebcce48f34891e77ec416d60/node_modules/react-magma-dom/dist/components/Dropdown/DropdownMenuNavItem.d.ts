import * as React from 'react';
import { IconProps } from 'react-magma-icons';
import { Omit } from '../../utils';
export interface DropdownMenuNavItemProps extends Omit<React.AnchorHTMLAttributes<HTMLAnchorElement>, 'color'> {
    /**
     * Leading icon for the menu item
     */
    icon?: React.ReactElement<IconProps>;
    /**
     * The href value of the link
     */
    to: string;
}
export declare const DropdownMenuNavItem: React.ForwardRefExoticComponent<DropdownMenuNavItemProps & React.RefAttributes<HTMLAnchorElement>>;
